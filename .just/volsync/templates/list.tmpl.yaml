---
apiVersion: batch/v1
kind: Job
metadata:
  name: "list-${app}-${ts}"
  namespace: "${ns}"
spec:
  ttlSecondsAfterFinished: 3600
  template:
    spec:
      automountServiceAccountToken: false
      restartPolicy: OnFailure
      containers:
        - name: list
          image: docker.io/restic/restic:0.19.0@sha256:7f44e0057b82348597568ea209360762d0b38f8e1dbc8ad859661ac1055e45f2
          args: ["snapshots"]
          envFrom:
            - secretRef:
                name: "${app}-volsync-secret"
