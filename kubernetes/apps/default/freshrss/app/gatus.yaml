---
apiVersion: v1
kind: ConfigMap
metadata:
  name: freshrss-gatus-ep
  namespace: default
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: freshrss
        group: internal
        url: https://freshrss.${SECRET_CLUSTER_DOMAIN}/i/
        interval: 1m
        client:
          insecure: true
        conditions:
          - "[STATUS] == 200"
        alerts:
          - type: pushover

