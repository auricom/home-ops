---
apiVersion: v1
kind: ConfigMap
metadata:
  name: ghostfolio-gatus-ep
  namespace: default
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: ghostfolio
        group: internal
        url: https://portfolio.${SECRET_CLUSTER_DOMAIN}
        interval: 1m
        client:
          insecure: true
        conditions:
          - "[STATUS] == 200"
        alerts:
          - type: pushover

