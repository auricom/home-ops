---
apiVersion: v1
kind: ConfigMap
metadata:
  name: homebox-gatus-ep
  namespace: default
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: homebox
        group: internal
        url: https://homebox.${SECRET_CLUSTER_DOMAIN}
        interval: 1m
        client:
          insecure: true
        conditions:
          - "[STATUS] == 200"
        alerts:
          - type: pushover

