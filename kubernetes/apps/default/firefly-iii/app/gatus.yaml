---
apiVersion: v1
kind: ConfigMap
metadata:
  name: firefly-iii-gatus-ep
  namespace: default
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: firefly-iii
        group: internal
        url: https://cash.${SECRET_CLUSTER_DOMAIN}/login
        interval: 1m
        client:
          insecure: true
        conditions:
          - "[STATUS] == 200"
        alerts:
          - type: pushover

