---
apiVersion: v1
kind: ConfigMap
metadata:
  name: komga-gatus-ep
  namespace: default
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: komga
        group: internal
        url: https://komga.${SECRET_CLUSTER_DOMAIN}
        interval: 1m
        client:
          insecure: true
        conditions:
          - "[STATUS] == 200"
        alerts:
          - type: pushover

