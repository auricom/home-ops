---
# yaml-language-server: $schema=https://kubernetes-schemas.devbu.io/helmrelease_v2beta1.json
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: &app pushover-notifier-ankr-queries
  namespace: default
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 1.3.2
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  maxHistory: 3
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    controller:
      type: cronjob
      cronjob:
        concurrencyPolicy: Forbid
        schedule: "*/30 * * * *"
    image:
      repository: ghcr.io/auricom/python
      tag: rolling@sha256:89e1f835de9cdf6827ac56d3efed4fc83d29f54cba1f10da5b7b941a054a22c1
    command:
      - python
      - /app/script.py
    service:
      main:
        enabled: false
    env:
      POSTGRES_HOST: ${POSTGRES_HOST}
    envFrom:
      - secretRef:
          name: pushover-notifier-ankr-queries-secret
    podAnnotations:
      reloader.stakater.com/auto: "true"
    resources:
      requests:
        cpu: 50m
        memory: 250Mi
      limits:
        memory: 250Mi
    persistence:
      config:
        enabled: true
        type: configMap
        name: pushover-notifier-ankr-queries-configmap
        mountPath: /app/config.yaml
        subPath: config.yaml
      script:
        enabled: true
        type: configMap
        name: pushover-notifier-ankr-queries-configmap
        mountPath: /app/script.py
        subPath: script.py
