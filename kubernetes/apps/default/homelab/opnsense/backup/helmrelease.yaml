---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app homelab-opnsense-backup
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: homelab-opnsense-backup
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      strategy: rollback
      retries: 3
  values:
    controllers:
      homelab-opnsense-backup:
        type: cronjob
        cronjob:
          concurrencyPolicy: Forbid
          schedule: "@daily"
        containers:
          app:
            image:
              repository: ghcr.io/auricom/toolbox
              tag: rolling@sha256:a474d465f0c431cac0d78842d34e0d9be05122b3bcb4c93bd48d5f261a62dc38
            command:
              - /bin/bash
              - /app/backup.sh
            env:
              OPNSENSE_URL: "https://opnsense.feisar.ovh"
              S3_URL: "https://s3.xpander.ovh"
            envFrom:
              - secretRef:
                  name: homelab-opnsense-backup-secret
    service:
      app:
        controller: *app
        enabled: false
    persistence:
      config:
        enabled: true
        type: configMap
        name: homelab-opnsense-backup-configmap
        defaultMode: 0775 # trunk-ignore(yamllint/octal-values)
        globalMounts:
          - path: /app/backup.sh
            subPath: backup.sh
            readOnly: true
      nfs:
        type: nfs
        server: 192.168.18.10
        path: /mnt/vol1/apps/homelab_backups/opnsense
        globalMounts:
          - path: /mnt/nfs/opnsense
