---
apiVersion: v1
kind: ConfigMap
metadata:
  name: immich-gatus-ep
  namespace: default
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: immich
        group: internal
        url: https://photos.${SECRET_CLUSTER_DOMAIN}/auth/login
        interval: 1m
        client:
          insecure: true
        conditions:
          - "[STATUS] == 200"
        alerts:
          - type: pushover

