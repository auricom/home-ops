---
apiVersion: v1
kind: ConfigMap
metadata:
  name: babybuddy-gatus-ep
  namespace: default
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: babybuddy
        group: internal
        url: https://babybuddy.${SECRET_CLUSTER_DOMAIN}/login/
        interval: 1m
        client:
          insecure: true
        conditions:
          - "[STATUS] == 200"
        alerts:
          - type: pushover

