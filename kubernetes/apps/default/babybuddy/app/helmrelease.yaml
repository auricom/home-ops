---
# yaml-language-server: $schema=https://raw.githubusercontent.com/fluxcd-community/flux2-schemas/main/helmrelease-helm-v2beta1.json
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: &app babybuddy
  namespace: default
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 1.5.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  maxHistory: 2
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    initContainers:
      01-init-db:
        image: ghcr.io/auricom/postgres-init:15.4@sha256:e6061831a8ab598ca770f91df21cd37cc87217be2c6314ca415afa0c6986cc39
        imagePullPolicy: IfNotPresent
        envFrom: &envFrom
          - secretRef:
              name: babybuddy-secret
      02-migrations:
        image: ghcr.io/auricom/babybuddy:2.0.4@sha256:01061536dffec41198e7d28da9911aa2b6b3bf2917b01d6e19e0e321bfe696fd
        imagePullPolicy: IfNotPresent
        envFrom: *envFrom
        command:
          - "/bin/bash"
          - "-c"
          - |
            #!/bin/bash

            set -o errexit
            set -o nounset

            python3 www/public/manage.py migrate --noinput
            python3 www/public/manage.py createcachetable
    controller:
      annotations:
        reloader.stakater.com/auto: "true"
    image:
      repository: ghcr.io/auricom/babybuddy
      tag: 2.0.4@sha256:01061536dffec41198e7d28da9911aa2b6b3bf2917b01d6e19e0e321bfe696fd
    service:
      main:
        ports:
          http:
            port: 3000
    env:
      TZ: ${TIMEZONE}
      EMAIL_HOST: smtp-relay.default.svc.cluster.local.
      EMAIL_PORT: "2525"
      EMAIL_USE_TLS: "false"
    envFrom: *envFrom
    persistence:
      config:
        enabled: true
        existingClaim: babybuddy-config
    ingress:
      main:
        enabled: true
        ingressClassName: "nginx"
        annotations:
          hajimari.io/icon: mdi:baby-face-outline
        hosts:
          - host: &host "babybuddy.${SECRET_CLUSTER_DOMAIN}"
            paths:
              - path: /
                pathType: Prefix
        tls:
          - hosts:
              - *host
    resources:
      requests:
        cpu: 100m
        memory: 256Mi
