---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app filebrowser
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      strategy: rollback
      retries: 3
  values:
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
    controllers:
      filebrowser:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: docker.io/filebrowser/filebrowser
              tag: v2.56.0@sha256:2c54423548d030a0613e144151d7783eb43321a3a79a1ba291c24b4198c048b1
            env:
              FB_DATABASE: /config/filebrowser.db
              FB_ROOT: /mnt
              FB_LOG: stdout
              FB_PORT: &port 7000
            probes:
              liveness: &probes
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /health
                    port: *port
                  initialDelaySeconds: 0
                  periodSeconds: 10
                  timeoutSeconds: 1
                  failureThreshold: 3
              readiness: *probes
            resources:
              requests:
                cpu: 100m
              limits:
                memory: 2Gi
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop:
                  - ALL
    service:
      app:
        controller: *app
        ports:
          http:
            port: *port
    route:
      app:
        hostnames: ["{{ .Release.Name }}.${SECRET_EXTERNAL_DOMAIN}"]
        parentRefs:
          - name: internal
            namespace: network
            sectionName: https
        rules:
          - backendRefs:
              - name: *app
                port: *port
    persistence:
      config:
        enabled: true
        existingClaim: *app
        globalMounts:
          - path: /config
      booklore:
        enabled: true
        type: nfs
        server: 192.168.9.10
        path: /mnt/vol1/apps/booklore
        globalMounts:
          - path: /mnt/vol1/apps/booklore
      bookstack:
        enabled: true
        type: nfs
        server: 192.168.9.10
        path: /mnt/vol1/apps/bookstack
        globalMounts:
          - path: /mnt/vol1/apps/bookstack
      homelab-backups:
        enabled: true
        type: nfs
        server: 192.168.9.10
        path: /mnt/vol1/apps/homelab_backups
        globalMounts:
          - path: /mnt/vol1/apps/homelab_backups
      paperless:
        enabled: true
        type: nfs
        server: 192.168.9.10
        path: /mnt/vol1/apps/paperless
        globalMounts:
          - path: /mnt/vol1/apps/paperless
      downloads:
        enabled: true
        type: nfs
        server: 192.168.9.10
        path: /mnt/vol1/downloads
        globalMounts:
          - path: /mnt/vol1/downloads
      claude:
        enabled: true
        type: nfs
        server: 192.168.9.10
        path: /mnt/vol1/home/claude
        globalMounts:
          - path: /mnt/vol1/home/claude
      helene:
        enabled: true
        type: nfs
        server: 192.168.9.10
        path: /mnt/vol1/home/helene
        globalMounts:
          - path: /mnt/vol1/home/helene
      music:
        enabled: true
        type: nfs
        server: 192.168.9.10
        path: /mnt/vol1/music
        globalMounts:
          - path: /mnt/vol1/music
      music-transcoded:
        enabled: true
        type: nfs
        server: 192.168.9.10
        path: /mnt/vol1/music_transcoded
        globalMounts:
          - path: /mnt/vol1/music_transcoded
      photo:
        enabled: true
        type: nfs
        server: 192.168.9.10
        path: /mnt/vol1/photo
        globalMounts:
          - path: /mnt/vol1/photo
      piracy:
        enabled: true
        type: nfs
        server: 192.168.9.10
        path: /mnt/vol1/piracy
        globalMounts:
          - path: /mnt/vol1/piracy
      shared-documents:
        enabled: true
        type: nfs
        server: 192.168.9.10
        path: /mnt/vol1/shared-documents
        globalMounts:
          - path: /mnt/vol1/shared-documents
      video:
        enabled: true
        type: nfs
        server: 192.168.9.10
        path: /mnt/vol1/video
        globalMounts:
          - path: /mnt/vol1/video
