---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./helm-release.yaml
  - ./volume.yaml
configMapGenerator:
  - name: resilio-claude-sync-conf
    namespace: default
    files:
      - ./config/sync.conf
generatorOptions:
  disableNameSuffixHash: true
