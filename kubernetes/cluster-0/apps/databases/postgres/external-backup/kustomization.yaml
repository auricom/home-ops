apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./helm-release.yaml
configMapGenerator:
  - name: postgres-external-backup
    files:
      - ./00-webhook
generatorOptions:
  disableNameSuffixHash: true
