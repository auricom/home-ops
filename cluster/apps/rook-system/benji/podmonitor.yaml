---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: benji
  namespace: rook-ceph
spec:
  podMetricsEndpoints:
    - interval: 4m
      path: /metrics
      port: metrics
      scrapeTimeout: 30s
  selector:
    matchLabels:
      app: pushgateway
      release: benji
