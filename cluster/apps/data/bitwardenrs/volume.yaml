---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bitwardenrs-config
  namespace: data
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: rook-ceph-block
  resources:
    requests:
      storage: 1Gi
