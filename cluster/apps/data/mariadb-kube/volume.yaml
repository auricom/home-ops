---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mariadb-kube
  namespace: data
  labels:
    kasten-io-snapshots: "enable"
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: rook-ceph-block
  resources:
    requests:
      storage: 10Gi
