---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pyload-config
  namespace: media
  labels:
    benji-backup.me/instance: "benji-k8s"
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: rook-ceph-block
  resources:
    requests:
      storage: 1Gi
